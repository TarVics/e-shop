export interface BrandDbType {
    id: string;
    name: string;
    createdAt: string;
    changedAt: string;
}
export interface BrandType {
    id: string;
    name: string;
    createdAt: string;
    changedAt: string;
}
