export interface SizeDbType {
    id: string;
    name: string;
    createdAt: string;
    changedAt: string;
}

export interface SizeType {
    id: string;
    name: string;
    createdAt: string;
    changedAt: string;
}
